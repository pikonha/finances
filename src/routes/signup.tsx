import { createFileRoute, Link, useRouter } from '@tanstack/react-router'; import { useState } from 'react'; import { authClient } from '#/lib/auth-client'; import { Button } from '@/components/ui/button'; import { Card,CardContent,CardDescription,CardHeader,CardTitle } from '@/components/ui/card'; import { Input } from '@/components/ui/input'; import { Label } from '@/components/ui/label'
export const Route=createFileRoute('/signup')({component:Signup})
function Signup(){const router=useRouter(),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');return <main className='page-wrap flex min-h-[70vh] items-center justify-center py-10'><Card className='w-full max-w-md'><CardHeader><CardTitle>Create account</CardTitle><CardDescription>Your data stays isolated to your user.</CardDescription></CardHeader><CardContent><form className='space-y-4' onSubmit={async e=>{e.preventDefault();setError('');const result=await authClient.signUp.email({name,email,password});if(result.error){setError(result.error.message||'Unable to sign up');return}await router.invalidate();await router.navigate({to:'/'})}}><div className='space-y-2'><Label>Name</Label><Input value={name} onChange={e=>setName(e.target.value)} required/></div><div className='space-y-2'><Label>Email</Label><Input type='email' value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className='space-y-2'><Label>Password</Label><Input type='password' minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></div>{error&&<p className='text-sm text-destructive'>{error}</p>}<Button className='w-full'>Create account</Button><p className='text-center text-sm text-muted-foreground'>Have an account? <Link to='/login'>Sign in</Link></p></form></CardContent></Card></main>}
